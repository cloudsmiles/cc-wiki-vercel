# scan命令

## Scan命令

### 介绍

```json
SCAN cursor [MATCH pattern] [COUNT count] [TYPE type]
```

Scan 提供了三个参数，第一个是 cursor 整数值，第二个是 key 的正则模式，第三个是遍历的 limit hint。第一次遍历时，cursor 值为 0，然后将返回结果中第一个整数值为下一次遍历的 cursor，一直遍历到返回的 cursor 值为 0 时结束。

### 特点

- 复杂度虽然也是 O(n)，但它是通过游标分布进行的，不会阻塞线程。
- 提供 limit 参数，可以控制每次返回结果的最大条数，limit 只是一个 hint。
- 同 keys 一样，它也提供模式匹配功能。
- 服务器不需要为游标保存状态，游标的唯一状态就是 scan 返回给客户端的游标整数。
- 返回的结果可能会有重复，需要客户端去重，这点非常重要。
- 遍历的过程中如果有数据修改，改动后的数据能不能遍历到时不确定的。
- 单次返回的结果是空的并不意味着遍历结束，而要看返回的游标值是否为零。

## 原理

### 字典的结构

在 Redis 中所有的 key 都存储在一个很大的字典里，这个字典的结构和 Java 中的 HashMap 一样，如图：

![Untitled](RPG从零开始/Database/Redis/实际应用/scan命令/Untitled.png)

它是一维数组，是二维链表结构。第一维数组的大小总是 2n(n >= 0)，扩容一次数组，大小空间加倍，也就是 2n+1。

Scan 指令返回的游标就是第一维数组的位置索引，将这个位置索引称为槽（slot）。如果不考虑字典的扩容缩容，直接按数组下标挨个遍历就行了。limit 参数就表示需要遍历的槽数，之所以返回的结果可能多可能少，是因为不是所有的槽位上都会挂链表，有些槽位可能是空的，有些槽位上挂接的链表上的元素可能会有多个。每一次遍历都会将 limit 数量的槽位上挂接的所有链表元素进行模式匹配过滤后，一次性返回给客户端。

### Scan遍历顺序

Scan 的遍历顺序非常特表。它不是从第一维数组的第 0 位一致遍历到末尾，而是采用了高位进位加法来遍历。之所以使用这样特殊的方式进行遍历，是考虑到字典的扩容和缩容时避免槽位的遍历重复和遗漏。

首先根据下图，来区分普通加法和高位进位加法的区别：

![Untitled](RPG从零开始/Database/Redis/实际应用/scan命令/Untitled%201.png)

从图中可以看出高位进位加法从左边加，进位往右边一点，同普通加法正好相反。但它们始终都会遍历所有的槽位并且没有重复。

### 字典扩容

Java 中的 HashMap 有扩容的概念，当 LoadFactor 达到阈值时，需要重新分配一个新的 2 倍大小的数组，然后将所有的元素全部 rehash 挂到新的数组下面。rehash 就是将元素的 hash 值对数组长度进行取模运算，因为长度变了，所以每个元素挂接的槽位可能也发生了变化。又因为数组的长度是 2 的 n 次方，所以取模运算等价于位于操作。

```json
a mod 8 = a & (8 - 1) = a & 7
a mod 16 = a & (16 - 1) = a & 15
a mod 32 = 1 & (32 - 1) = a & 31
```

这里的 7、15、31 称为字典的 mask 值，mask 的作用就是保留 hash 值的低位，高位都被设置为 0。

接下来看 rehash 前后元素槽位的变化。如下图：

![Untitled](RPG从零开始/Database/Redis/实际应用/scan命令/Untitled%202.png)

假设当前的字典的数组长度由 8 位扩容到 16 位，那么 3 号槽位 011 将会被 rehash 到 3 号槽位和 11 号槽位，也就是说该槽位链表中大约有一半的元素还是 3 号槽位，其他的元素会被放到 11 号槽位，11 这个数字的二进制是 1011，就是对 3 的二进制 011 增加了一个高位 1。

抽象一点说，假设开始槽位的二进制数是 xxx，那么该槽位中的元素将被 rehash 到 0xxx 和 1xxx(xxx+8) 中。如果字典长度由 16 位扩容到了 32 位，那么对于二进制槽位 xxx 中的元素将被 rehash 到 0xxxx 和 1xxxx(xxxx + 16) 中。

### 对比扩容、缩容前后的遍历顺序

如下图：

![Untitled](RPG从零开始/Database/Redis/实际应用/scan命令/Untitled%203.png)

设当前要遍历 110 这个位置（橙色），那么扩容后，当前槽位所有的元素对应的新槽位是 0110 和 1110（深绿色），也就是在槽位的二进制数增加一个高位 0 或 1.这时可以直接从 0110 这个槽位开始往后继续遍历，0110 槽位之前的所有槽位都是一件遍历过的，这样就可以避免扩容后对已经遍历过的槽位进行重复遍历。

在考虑缩容，假设当前即将遍历 110 这个位置（橙色），那么缩容后，当前槽位所有的元素对应的新槽位 10（深绿色），也就是去掉槽位二进制最高位。这时可以直接从 10 这个槽位继续往后遍历，10 槽位之前的所有槽位之前的所有槽位都已经遍历过的，这样就可以避免缩容的重复遍历。不过缩容还是不太一样，它会对图中 010 这个槽位上的元素进行重复遍历，因为缩容后 10 槽位的元素是 010 和 110 上挂接的元素的融合。

### 渐进式 rehash

Java 的 HashMap 在扩容时会一次性将旧数组下挂接的元素全部转移到新数组下面。如果 HashMap 中元素特别多，线程就会出现卡顿现象。Redis 为了解决这个问题，采用 “渐进式 rehash”。

它会同时保留旧数组和新数组，然后在定时任务中以及后续对  hash 的指令操作中渐渐地将旧数组中挂接的元素迁移到新数组上。这意味着要操作处于 rehash 中的字典，需要同时访问新旧两个数组结构。如果在旧数组下面找不到元素，还需要去新数组下面寻找。

Scan 也需要考虑这个问题，对于 rehash 中的字典，它需要同时扫描新旧槽位，然后将结果融合后返回给客户端。

### 更多的 Scan 指令

Scan 指令是一系列指令，除了可以遍历所有的 key 之外，还可以对指定的容器集合进行筛选。比如 zscan 遍历 zset 集合元素，hscan 遍历 hash 字典的元素，sscan 遍历 set 集合的元素。

它们的原理同 scan 类似，因为 hash 底层就是字典，set 也是一个特殊的 hash（所有的 value 指向同一个元素），zset 内部也使用了字典来存储所有的元素内容。

## Scan Count Performance

COUNT 是每次游标迭代时要搜索的键数。因此，计数越小，给定数据集所需的增量迭代越多。下面的图表显示了使用各种 COUNT 设置遍历 500 万个键的数据集的执行时间。

![Untitled](RPG从零开始/Database/Redis/实际应用/scan命令/Untitled%204.png)

可以注意到，对于非常小的计数大小，迭代所有键所需的时间会显着增加。分组到更大的数量会导致 SCAN 完成的延迟更短。批量大小为 1000 或更大时，返回结果的速度要快得多。

## **Performance Degradation with SCAN**

但是问题仍然存在，SCAN 将如何影响生产负载？如果我的 COUNT 大小足够小，我可以一次运行大量 SCAN 查询吗？
下图显示了在 COUNT 大小为 100、1000 和 10,000 的大型数据集上运行 SCAN 的效果。它显示了运行查询对现有工作负载性能的影响。

![Untitled](RPG从零开始/Database/Redis/实际应用/scan命令/Untitled%205.png)

对于上述内容，memtier 用于生成工作负载，并运行特定数量的 SCAN 查询以查看对工作负载的影响。
不运行 SCAN 的基线操作数/秒约为 128,000 操作数/秒。很明显，随着执行更多的 SCAN 查询，工作负载吞吐量会大大降低。许多迭代的累积阻塞效应很快变得明显。这意味着减少正在执行的 SCAN 查询的数量以防止性能急剧下降是很重要的。

[Redis Scan - Redis - Wiki.Shileizcc.com](https://wiki.shileizcc.com/confluence/display/RED/Redis+Scan)

[The effects of Redis SCAN on performance and how KeyDB improved it | KeyDB - The Faster Redis Alternative](https://docs.keydb.dev/blog/2020/08/10/blog-post/)

[Redis SCAN的使用 - 静中细思的博客 | Kingnet Blog](https://jinguoxing.github.io/redis/2018/09/04/redis-scan/)