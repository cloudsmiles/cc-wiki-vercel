# 重构方案

1.custom CUD

- [x]  create service
- [x]  upsert service， create user-service
- [x]  update service

[2.company](http://2.company) CUD

- [x]  create company

3.用户账号CRUD

- [x]  create user
- [x]  upsert user
- [x]  update user

4.用户子账号CRUD

- [ ]  create subaccount
- [ ]  delete subaccount


- [x]  review代码
- [x]  修复迁移代码bug
- [x]  zim代码合并
- [x]  申请数据库权限
- [x]  数美错误码处理
    - [x]  代码review
    - [x]  beta环境
    - [x]  数据库lang_key增加记录
- [x]  浩强数据查询
- [x]  公司认证增加字段
    - [x]  代码review
- [ ]  重构代码
    - [x]  利用gorm的callback，重新整理model
    - [x]  华为云子账号
    - [x]  更新企业信息
    - [x]  bind or unbind企业，更新客户信息，触发财务公司同步

重构代码修改点整理：
1.梳理财务公司同步场景
~~2.去掉model的option参数~~
~~3.在钩子只放必要的逻辑，增加参数校验~~
~~4.验证updated_at, created_at~~

只有updateColumn，updateColumns不行
~~5.上下文context验证，是否创建新连接，是否会复用旧的context~~

不会复用旧的context，除非指定statement赋值

新建会话只是clone实例，不会对底层mysql连接有影响
~~6.公司名称修改，是否更新user_info~~
~~7.getReParms都统一放在一个函数里~~
~~8.命名规范，updateUserInfo去除userInfo~~
~~9.gorm是否有办法不覆盖旧的值~~

select count(*) from cloud_market_account a inner join service_appid b on a.s_id=b.s_id inner join cloud_market_app c on b.app_id=c.app_id where a.product_id=2 and c.product_id=2;